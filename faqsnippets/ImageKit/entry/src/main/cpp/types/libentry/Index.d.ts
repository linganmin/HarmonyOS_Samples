import { image } from "@kit.ImageKit";

export const arrayBufferToMat: (pixelMap: image.PixelMap, readBuffer: ArrayBuffer, dir: string) => void;

export const accessToMat: (pixelMap: image.PixelMap, dir: string) => void;